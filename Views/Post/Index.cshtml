@model IEnumerable<FurryFriendsMVC.Models.Post.PostListItem>
   
@{
    ViewData["Title"] = "Post Index";
}
<h1>Post Stuff?</h1>
<p>
    <a asp-action="Create">Create Post</a>
    <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateTimeCreated)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateTimeUpdated)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
            @foreach (var item in Model)
            {
    <tr>
        <td>
                        @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
                        @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
                        @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
                        @Html.DisplayFor(modelItem => item.DateTimeCreated)
        </td>
        <td>
                        @Html.DisplayFor(modelItem => item.DateTimeUpdated)
        </td>
        <td>
 @Html.ActionLink("Edit", "Edit", "Post", new { id = item.Id }) |
                    @Html.ActionLink("Detail", "Detail", "Post", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", "Post", new { id = item.Id })
        </td>
    </tr>
            }
    </tbody>
</table>
</p>